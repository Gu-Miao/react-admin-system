import { createStyles } from '@mantine/core'

export default createStyles(() => ({
  container: {
    height: '100vh',
    textAlign: 'center',
    background: '#e9e9e9',
    padding: '20vh 30px'
  },
  banner: {
    width: '100%',
    maxWidth: 500,
    height: 'auto',
    marginBottom: 50
  },
  title: {
    marginBottom: 20
  }
}))
